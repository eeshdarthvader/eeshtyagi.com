{
  "name": "eeshtyagi.com",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lh": "node node_modules/lighthouse-pwa/lighthouse-cli https://www.eeshtyagi.com --quiet --chrome-flags='--no-sandbox --headless' --output json"
  },
  "devDependencies": {
    "lighthouse-pwa": "^0.1.0"
  }
}
